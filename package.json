{
  "name": "noxpay",
  "private": true,
  "version": "0.1.0",
  "description": "Private Web3 freelancer escrow powered by iExec Nox Confidential Tokens.",
  "workspaces": [
    "contracts",
    "frontend"
  ],
  "scripts": {
    "build": "npm run build --workspace contracts && npm run build --workspace frontend",
    "compile": "npm run compile --workspace contracts",
    "dev": "npm run dev --workspace frontend",
    "test": "npm run test --workspace contracts"
  },
  "engines": {
    "node": ">=20"
  }
}
